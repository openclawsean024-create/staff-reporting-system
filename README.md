# Staff Reporting System

Google Apps Script 員工報工系統。

## 目前版本

- `GoogleAppsScript.js` — 目前主要版本

## 歷史版本

已封存至 `archive/`，包含 v3～v13、各階段命名版本（final、fixed、simple、price、tz、admin 等）。

## 結構

```
/
├── GoogleAppsScript.js   # 主要邏輯
├── index.html            # Web UI
├── package.json          # Vercel deployment config
├── vercel.json
├── api/                  # Vercel API routes
├── archive/              # 歷史版本（不活躍）
└── .gitignore
```
